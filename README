# Boobank PHP

tools for weboob/boobank usage in php

## dependencies
 - apt-get install weboob weboob-qt
 - weboob-config-update

default boobank command at /usr/bin
 but you can define another bin path in parameters.yml
  boobank:
        bin_path:"/usr/bin"
        
## registered commands
- boobank:backend:list